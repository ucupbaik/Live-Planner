# Turso database credentials (server-side only — never expose to the browser)
# Get these from your Turso dashboard. The token must have read (rw is fine) access.
TURSO_URL=libsql://your-db.turso.io
TURSO_TOKEN=your-token-here
